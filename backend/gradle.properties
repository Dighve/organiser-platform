# Java Configuration
# Note: org.gradle.java.home is not set to allow Gradle toolchain to work properly
# The Java version is controlled by build.gradle toolchain configuration (Java 17)

# Gradle configuration
org.gradle.jvmargs=--enable-preview -Xmx2g -XX:MaxMetaspaceSize=1g -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
dorg.gradle.daemon=true
org.gradle.configureondemand=true

# Java toolchain configuration
org.gradle.java.installations.auto-detect=true
org.gradle.java.installations.auto-download=false

# Enable Gradle's configuration cache
owners.readonly=true

# Kotlin DSL settings
kotlin.code.style=official

# Enable JVM toolchain auto-provisioning
org.gradle.jvm.environment=local

# Enable build scan
o.gradle.enterprise.build-scan-plugin.version=3.13.4

# Disable build scan by default (uncomment to enable)
# com.gradle.enterprise.build-scan.publish=ALWAYS
# com.gradle.enterprise.build-scan.capture-task-input-files=true
